# Sin prefijo /api: /decide, /decisions, /status, /replays, /zones, /route y
# /shocks estan montados en la RAIZ del backend (ver backend/main.py). Si esto
# trae /api, el dashboard entero se queda pegado en "sin backend" -- ya paso.
NEXT_PUBLIC_API_URL=http://localhost:8000

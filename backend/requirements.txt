fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
pydantic==2.10.4
python-dotenv==1.0.1

# Ruteo / optimizacion (Bloque 4 y 5)
ortools==9.11.4210
osmnx==2.0.1
scikit-learn==1.9.1
networkx==3.4.2
pandas==2.2.3
numpy==2.2.1

# Dev / testing
pytest==8.3.4
httpx==0.28.1

# La capa de estrategia (tier2) habla con Gemini por HTTP con urllib de la
# biblioteca estandar: una sola peticion JSON corta, sin reintentos por
# diseño. Un SDK no aportaria nada que aqui se use y seria una dependencia
# mas que puede faltar en la maquina de la demo -- justo en el unico camino
# cuyo fallo hay que demostrar en vivo.

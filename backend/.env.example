FRONTEND_ORIGIN=http://localhost:3000
SIMULATION_SEED=42
SHIFT_DURATION_MINUTES=480
GRAPHML_PATH=data/monterrey.graphml

# Capa de estrategia (tier2). OPCIONAL: sin ella el sistema corre
# entero con NullAdvisor y decide igual de bien -- solo que `degraded`
# nunca es verdad porque no hay modelo que se caiga, y el requisito 7
# del protocolo de evaluacion queda sin demostrar.
ANTHROPIC_API_KEY=

# --- Python (backend) ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
backend/.env

# --- Node / Next.js (frontend) ---
node_modules/
frontend/.next/
frontend/out/
frontend/.env.local
frontend/.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/

# --- Datasets pesados (ver backend/data/README.md) ---
backend/data/*.graphml
backend/data/*.csv
# La tabla de resultados SI se commitea (1.2 KB): es la evidencia del criterio
# de Results, la sirve GET /results y el dashboard la dibuja, asi que tiene que
# existir en un clon limpio sin correr la evaluacion antes.
!backend/data/results_table.csv
# Descargas crudas de OSMnx/Overpass (53 MB y creciendo). Se regeneran solas;
# lo unico que hace falta conservar es data/route_cache.json, que pesa 263 KB
# y SI se commitea a proposito -- ver core/routing/osrm.py.
backend/data/cache/

# --- OS / editor ---
.DS_Store
.vscode/
.idea/
*.swp
backend/.venv/
backend/out/
backend/logs/
